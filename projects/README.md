# Projects

Portfolio-grade work you can pin on GitHub, run locally, and talk about in interviews or proposals.

| Folder | Focus | What to say |
|--------|--------|-------------|
| **`taskboard/`** | Full-stack + containers | End-to-end app: REST API, web UI, file persistence, Docker, automated tests, CI on every push. |
| **`aws-static-site/`** | IaC (Terraform) | Minimal AWS S3 static website; `terraform plan` before any apply; add CloudFront for real production. |
| **`.github/workflows/`** (repo root) | CI/CD | `taskboard-ci.yml` runs tests and builds the Docker image for `taskboard`. |

Next steps for you:

1. Push this repo, then pin **`projects/taskboard`** (and optionally **`aws-static-site`**) from your profile or split them into separate repos later.
2. Add a **live demo URL** on the portfolio when you deploy (e.g. Fly.io, Railway, ECS, or S3+CloudFront).
